import { Layout } from "../../components/Layout";

type Props = {
    tag:string
}

const Index = ({ tag }: Props)=>{
    
    return (
        <Layout>
            
        </Layout>
    );

}

export default Index;